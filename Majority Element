class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        candidate = None
        count = 0
        
        for num in nums:
            # Establish a new candidate if current count is depleted
            if count == 0:
                candidate = num
            
            # Vote adjustments
            count += 1 if num == candidate else -1
            
        return candidate
