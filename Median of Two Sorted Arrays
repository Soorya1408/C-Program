#include <limits.h>

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))

double findMedianSortedArrays(int* nums1, int nums1Size, int* nums2, int nums2Size) {
    // Ensure nums1 is the smaller array
    if (nums1Size > nums2Size) {
        return findMedianSortedArrays(nums2, nums2Size, nums1, nums1Size);
    }
    
    int low = 0;
    int high = nums1Size;
    int totalElements = nums1Size + nums2Size;
    
    while (low <= high) {
        int partition1 = (low + high) / 2;
        int partition2 = (totalElements + 1) / 2 - partition1;
        
        // Edge cases: if partition is 0 or at max size
        int maxLeft1 = (partition1 == 0) ? INT_MIN : nums1[partition1 - 1];
        int minRight1 = (partition1 == nums1Size) ? INT_MAX : nums1[partition1];
        
        int maxLeft2 = (partition2 == 0) ? INT_MIN : nums2[partition2 - 1];
        int minRight2 = (partition2 == nums2Size) ? INT_MAX : nums2[partition2];
        
        // Check if we found the correct partition
        if (maxLeft1 <= minRight2 && maxLeft2 <= minRight1) {
            // Odd total elements
            if (totalElements % 2 != 0) {
                return (double)MAX(maxLeft1, maxLeft2);
            }
            // Even total elements
            return (MAX(maxLeft1, maxLeft2) + MIN(minRight1, minRight2)) / 2.0;
        } 
        else if (maxLeft1 > minRight2) {
            // Move left in nums1
            high = partition1 - 1;
        } 
        else {
            // Move right in nums1
            low = partition1 + 1;
        }
    }
    
    return 0.0;
}
